org.gradle.jvmargs=-Xmx4g -Dfile.encoding=UTF-8
android.useAndroidX=true
kotlin.code.style=official
org.gradle.caching=true
# AGP 9: opt out of built-in Kotlin for now — the classic KGP + compose +
# serialization + KSP plugin combination is the documented, stable path.
# Revisit before AGP 10 (opt-out is removed there).
android.builtInKotlin=false
android.newDsl=false
